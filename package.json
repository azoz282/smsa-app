{
  "name": "smsa-shipment-app",
  "version": "1.0.0",
  "private": true,
  "description": "SMSA shipment form with fixed shipper and consignee addresses.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  }
}
